diff --git a/src/backends/drm/drm_egl_layer.cpp b/src/backends/drm/drm_egl_layer.cpp
index 9efd242608..f3511fd9c4 100644
--- a/src/backends/drm/drm_egl_layer.cpp
+++ b/src/backends/drm/drm_egl_layer.cpp
@@ -75,6 +75,7 @@ bool EglGbmLayer::doEndFrame(const QRegion &renderedRegion, const QRegion &damag
 
 bool EglGbmLayer::checkTestBuffer()
 {
+    m_scanoutBuffer.reset();
     return m_surface.renderTestBuffer(targetRect().size(), m_pipeline->formats(m_type), m_pipeline->output()->colorPowerTradeoff()) != nullptr;
 }
 
diff --git a/src/backends/drm/drm_gpu.cpp b/src/backends/drm/drm_gpu.cpp
index 2dd35108d7..c3a34e53a5 100644
--- a/src/backends/drm/drm_gpu.cpp
+++ b/src/backends/drm/drm_gpu.cpp
@@ -454,6 +454,12 @@ DrmPipeline::Error DrmGpu::testPipelines()
         // nothing to do
         return DrmPipeline::Error::None;
     }
+    // ensure we have suitable buffers for the test
+    for (DrmPipeline *pipeline : m_pipelines) {
+        if (pipeline->activePending() && !pipeline->primaryLayer()->checkTestBuffer()) {
+            return DrmPipeline::Error::InvalidArguments;
+        }
+    }
     QList<DrmPipeline *> inactivePipelines;
     std::ranges::copy_if(m_pipelines, std::back_inserter(inactivePipelines), [](const auto pipeline) {
         return pipeline->enabled() && !pipeline->active();
diff --git a/src/backends/drm/drm_pipeline.cpp b/src/backends/drm/drm_pipeline.cpp
index c81140c160..c0ebc26a75 100644
--- a/src/backends/drm/drm_pipeline.cpp
+++ b/src/backends/drm/drm_pipeline.cpp
@@ -202,8 +202,8 @@ DrmPipeline::Error DrmPipeline::prepareAtomicPresentation(DrmAtomicCommit *commi
         }
     }
 
-    if (!m_primaryLayer->checkTestBuffer()) {
-        qCWarning(KWIN_DRM) << "Checking test buffer failed!";
+    if (!m_primaryLayer->currentBuffer()) {
+        qCWarning(KWIN_DRM) << "An enabled plane has no buffer!";
         return Error::TestBufferFailed;
     }
     const auto fb = m_primaryLayer->currentBuffer();
diff --git a/src/backends/drm/drm_pipeline_legacy.cpp b/src/backends/drm/drm_pipeline_legacy.cpp
index ffe4f100be..7927249734 100644
--- a/src/backends/drm/drm_pipeline_legacy.cpp
+++ b/src/backends/drm/drm_pipeline_legacy.cpp
@@ -52,10 +52,10 @@ void DrmPipeline::forceLegacyModeset()
 
 DrmPipeline::Error DrmPipeline::legacyModeset()
 {
-    if (!m_primaryLayer->checkTestBuffer()) {
-        return Error::TestBufferFailed;
-    }
     const auto buffer = m_primaryLayer->currentBuffer();
+    if (!buffer) {
+        return Error::InvalidArguments;
+    }
     if (m_primaryLayer->sourceRect() != QRect(QPoint(0, 0), buffer->buffer()->size())) {
         return Error::InvalidArguments;
     }
